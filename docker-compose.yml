services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: docproc
      POSTGRES_USER: docproc
      POSTGRES_PASSWORD: docproc
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
