DATABASE_URL=postgresql+asyncpg://docproc:docproc@localhost:5433/docproc
APP_ENV=dev
LOG_LEVEL=INFO
OCR_PROVIDER=mock
EXTRACTION_MODE=llm  # set to "simple" to use pattern extraction
# Provide the OpenAI key only when running LLM extraction; never commit secrets.
OPENAI_API_KEY=<your-openai-api-key>
LLM_MODEL=gpt-4o-mini
# If you later switch OCR providers:
# OCR_PROVIDER=paddleocr
# OCR_PROVIDER=aws_textract
